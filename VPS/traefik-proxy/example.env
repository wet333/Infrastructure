# Let's Encrypt / ACME: email for certificate expiration notifications (required)
ACME_EMAIL=email@example.com

# Dashboard: hostname used to access the Traefik dashboard (e.g. traefik.mydomain.com)
# Create an A record pointing this hostname to your VPS IP.
DASHBOARD_HOST=traefik.example.com

# Dashboard: basic auth credentials (user:hashedpassword).
# Generate a hash with (install apache2-utils for htpasswd if needed):
#   htpasswd -nbB admin yourpassword
# Then set DASHBOARD_AUTH_USERS to the full output, e.g.:
#   DASHBOARD_AUTH_USERS=admin:$$2y$$05$$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# In .env, use $$ for each literal $ in the hash so Docker Compose doesn't treat it as a variable.
DASHBOARD_AUTH_USERS=admin:your_bcrypt_hash_here
